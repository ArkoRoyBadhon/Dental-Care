import { Box } from "@mui/material";

const Dashboard = () => {
    return (
        <Box
        sx={{
            paddingX: '60px'
        }}>
            
        </Box>
    );
};

export default Dashboard;